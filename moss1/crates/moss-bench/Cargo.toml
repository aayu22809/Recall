[package]
name = "moss-bench"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
moss-core = { path = "../moss-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

